<Page x:Name="Buildings" x:Class="WPF_стройка.Windows.BuildingsActionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Buildings">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top">
        <RadioButton x:Name="AddBuilding" Content="Добавить строение" Style="{StaticResource RadioButtonStyle}" />
        <RadioButton x:Name="RemoveBuilding" Content="Удалить строение" Style="{StaticResource RadioButtonStyle}" />
        <RadioButton x:Name="UpdateBuilding" Content="Обновить строение" Style="{StaticResource RadioButtonStyle}" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
            <Button Content="Выбрать"  Click="ChooseAction_Click" Style="{StaticResource ButtonStyle}"/>
            <Button Content="Назад"  Click="Back_Click" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </DockPanel>
</Page>
