<Page x:Name="Buildings" x:Class="WPF_стройка.Windows.GroupBuildingsActionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GroupBuildings">
    <StackPanel>
        <RadioButton x:Name="AddBuilding" Content="Добавить группу строений" Style="{StaticResource RadioButtonStyle}" Margin="10,20,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" />
        <RadioButton x:Name="RemoveBuilding" Content="Удалить группу строений" Style="{StaticResource RadioButtonStyle}" Margin="10,10,0,0" VerticalAlignment="Top" Height="18" />
        <RadioButton x:Name="UpdateBuilding" Content="Обновить группу строений" Style="{StaticResource RadioButtonStyle}" Margin="10,10,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" />
        <Button Content="Выбрать"  Click="ChooseAction_Click" Style="{StaticResource ButtonStyle}" Margin="0,50,0,0" Height="51" VerticalAlignment="Top"></Button>
        <Button Content="Назад"  Click="Back_Click" Style="{StaticResource ButtonStyle}" Height="51" VerticalAlignment="Bottom" Margin="0,20,0,0"/>
    </StackPanel>
</Page>
